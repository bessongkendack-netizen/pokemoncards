import { categories, searchProducts } from '@/lib/data';import { ProductCard } from '@/components/product-card';import { notFound } from 'next/navigation';
export default function Category({params}:{params:{slug:string}}){const c=categories.find(x=>x.slug===params.slug); if(!c) notFound(); const items=searchProducts('',c.name); return <section className="mx-auto max-w-7xl px-4 py-10"><h1 className="text-4xl font-black">{c.name}</h1><p className="mt-2 text-slate-500">{c.description}</p><div className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-4">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
