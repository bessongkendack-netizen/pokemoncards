import type { Metadata } from 'next';import './globals.css';import { AppProviders } from '@/providers/app-providers';import { Header } from '@/components/header';import { siteConfig } from '@/config/site';
export const metadata: Metadata={metadataBase:new URL(siteConfig.url),title:{default:'AnimeVault | Premium Anime Trading Cards',template:'%s | AnimeVault'},description:siteConfig.description,openGraph:{title:'AnimeVault',description:siteConfig.description,type:'website'},twitter:{card:'summary_large_image'}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en" suppressHydrationWarning><body><AppProviders><Header/><main>{children}</main><footer className="mx-auto max-w-7xl px-4 py-12 text-sm text-slate-500">© 2026 AnimeVault. Authenticated collectibles marketplace.</footer></AppProviders></body></html>}
