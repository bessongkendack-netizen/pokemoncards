import { ProductCard } from '@/components/product-card';import { searchProducts, categories } from '@/lib/data';
export default function Shop({searchParams}:{searchParams:{q?:string;category?:string}}){ const items=searchProducts(searchParams.q,searchParams.category); return <section className="mx-auto max-w-7xl px-4 py-10"><h1 className="text-4xl font-black">Shop Anime Cards</h1><form className="my-6 grid gap-3 md:grid-cols-3"><input className="input" name="q" placeholder="Search by card, SKU, rarity"/><select className="input" name="category"><option value="all">All categories</option>{categories.map(c=><option key={c.id}>{c.name}</option>)}</select><button className="btn">Search</button></form><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
